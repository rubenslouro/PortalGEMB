@* @using UtilService
@using UtilService.Util *@
@model List<Domain.Entities.Presenca>
@{
    ViewData["Title"] = "Lista de Presença";
    Layout = "~/Areas/Administrativo/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-md-12 top-bar">
        <a title="Clique aqui para lançar nova presença a alunos" class="btn btn-primary pull-right" href="@Url.Action("Adicionar","ConfiguracaoGeral", new { Area = "Administrativo"})"><i class="fa fa-edit"></i> Adicionar</a>
    </div>
</div>
<fieldset>
    <legend>Frequência</legend>
    <div class="row">
        <div class="col-xs-12 col-sm-6 col-md-4">
            <div class="form-group">
                @* 
                @Html.LabelFor(x => x.Pres_ID_Turma, null, new { @class = "control-label" })
                @Html.DropDownListFor(x => x.Pres_ID_Turma, (IEnumerable<SelectListItem>)ViewData["Turma"], string.Empty, new { @class = "form-control", id = "ddlTurma" })
                @Html.ValidationMessageFor(x => x.Pres_ID_Turma, null, new { @class = "alerta-campo" }) 
                *@
            </div>
        </div>
    </div>
</fieldset>
<div class="row">
    <div class="col-lg-12">
        <table class="table  table-hover" width="100%" data-table="true">
            <thead>
                <tr>
                    <th>Código</th>
                    <th>Turma</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
            <thead>
                <tr>
                    <td colspan="4">&nbsp;</td>
                </tr>
            </thead>
            <tfoot></tfoot>
        </table>
    </div>
</div> 

